#!/usr/bin/env bash

[[ $- != *i* ]] && return

if [ -f "/etc/bash.bashrc" ]; then
        . "/etc/bash.bashrc"
fi

# Clear console
clear() {
        command clear
        printf '\e[3J'
}
alias cls='clear'

PS1="\e[38;5;46m\]\w \$\e[0m\] "
PROMPT_DIRTRIM=2
